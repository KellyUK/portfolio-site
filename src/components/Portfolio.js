import React from "react";

const Portfolio = () => {
  return (
    <div>
      <section>
        <h2>Northcoders News</h2>
        <p>
          A reddit style news aggregation site where readers can view articles
          by topic. Each article can be rated up or down and users can leave
          comments. Comments can also be deleted by their author. The front end
          is built in React with Reach Router and using CSS Modules Stylesheets.
          It accesses a server that I build in Node.js with Express and Knex
        </p>
      </section>
    </div>
  );
};

export default Portfolio;
